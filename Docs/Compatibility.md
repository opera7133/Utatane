# ゴースト互換状況

「対応」と書いてあっても、全機能の完全再現ではありません。起動と基本会話を中心に見ています。

| ゴースト | SHIORI | 状況 | 主な残りもの |
| --- | --- | --- | --- |
| Emily/Phase4.5 | YAYA | 起動、会話、マウス反応、3キャラクター表示、メニュー画像の透過を確認 | 確認範囲では問題報告なし。全SakuraScript・SERIKOの再現は未保証 |
| めもりーな | SATORI | 起動、会話、SSU、システム情報をテスト済み | `exec.dll`、`proxy.dll`など外部処理 |
| ユグドラシルチェリィ | SATORI | 起動、2キャラクター、シェル切り替えを確認。起動・再読込時の表示、まばたきの重なり、HTTP更新失敗を修正後、利用者確認。「閉じる」の空応答でもバルーンを閉じる処理を自動テスト確認 | 「閉じる」の実アプリ再確認。`ukastream.dll`を使うルーレット |
| Water Surface | SATORI | 基本会話、キーワード取得、お気に入り登録、アンカー割り込み後の会話継続を確認 | ネットワーク更新は個別未確認 |
| 坊ちゃんの愛馬 | SATORI | 基本会話、3キャラクター表示、アンカー、選択肢とお気に入り等のメニュー配置を確認 | APNGは静止画として読込。全着せ替え・派生メニューは未確認 |
| ふくふくアフターライフ（仮） | SATORI | 基本動作、ゴースト切り替え時の表示消去、クリック待ち終了を確認 | 追加シェルは未確認 |
| 酒の神さま | SATORI | 起動と`kenonoke.dll`のキーワード分類をテスト済み | 音声系SAORI、画面効果 |
| COLORSβ | KAWARI | 起動、シェル、クリップボードをテスト済み | COLORS専用SAORIは後回し |
| Aosora demo | Aosora | 外部macOSモジュールで実際に起動確認済み | 上流変更への追従、配布方法 |
| 伺かWizまゆら | KAWARI(Legacy) | 起動、会話、部位別反応を実アプリで確認 | 確認範囲では問題報告なし |
| 偽さくら Rebirth 2.008 | ese-shiori + ParticleMakoto | 独自辞書の復号とese-shioriの評価処理、内部会話タイマー、他ゴースト検索・会話転送、辞書・スタック保存、Materia形式のニュース読込、ParticleMakotoをネイティブ実装。起動、定期会話、初回名前入力と選択肢、ダブルクリックメニュー、単語学習と再読込をテスト済み | 原版独自の文章分解・再構成アルゴリズムとの完全一致、長時間の状態変化は未確認 |
| こだま | 忍 | `shino.dll`を使わずネイティブ実行。起動、ランダムトーク、単語群と偽AIトーク、クリックと連続クリック、ダブルクリックメニュー、選択肢ジャンプ、状態保存をテストし、利用者がDebug版で会話を確認。SAORIは共通ネイティブ／外部モジュール経路へ接続 | コミュニケート相手・応答Referenceの設定、Windows操作・HWND・詳細なシステム情報、日時判定書式など。全分岐は未確認 |
| ゴス路吹雪 | 翡翠 | `hisui.dll`を使わずネイティブ実行。起動、時間帯分岐、自由トーク、当たり判定付きダブルクリック、メニューと選択肢、会話間隔、変数保存、リソース応答をテスト。利用者がDebug版で表示・会話・メニュー操作を確認 | 感情・学習モデル、未使用の組み込み関数、SAORI。全機能・全分岐は未確認 |
| さくら | Materia first | 対応版`first.dll`をWineなしで起動。会話、クリック、眠気・睡眠・入浴、主要メニューと状態保存を確認 | Materia固有のニュース、メール、Windows終了・再起動など |
| もす | KAGARI | KAGARI部分は動作可能 | Lua C拡張やSAORIはmacOS版を作成できた場合のみ利用可 |

## コンテンツのネットワーク更新

旧来の配布サイトとの互換性のためHTTP更新を許可します。HTTPでは更新一覧とファイルのMD5照合だけでは通信経路上の改ざんを防げません。信頼する配布元を利用してください。
HTTPSの証明書検証は無効化しません。アプリ自身の更新はHTTPS・Sparkle署名検証を維持します。

## SAORI

| モジュール | 状況 | 補足 |
| --- | --- | --- |
| SSU | 対応 | SATORI同梱実装 |
| `saori_cpuid.dll` | 対応 | macOSの情報を返す互換実装 |
| `kenonoke.dll` | 対応 | `keyword.txt`による分類 |
| `textcopy2.dll` | 対応 | macOSのクリップボードへ書き込み |
| `mciaudior.dll` | 対応 | AVFoundationによる`load`、`play`、`loop`、`stop` |
| `wmove.dll` | 一部対応 | `MOVETO`、`MOVETO_INSIDE`、`GET_POSITION`、`GET_DESKTOP_SIZE` |
| `exec.dll` / `process.dll` / `proxy.dll` | 未対応 | 権限と安全な挙動を決めてから |
| その他の標準SAORI DLL | 条件付き | 内蔵実装を優先し、汎用DLLホストとWineの設定時だけ実行。実DLL確認は未実施 |

実ゴーストを手元へ置く場所やテスト方法は[開発ガイド](Development.md)と[Native SHIORI / SAORI](Native-SHIORI.md)を参照してください。

表にあるネイティブSAORIは、SSUを除いてSHIORI非依存の共通レジストリにあり、ネイティブMISAKA、YAYA、SATORI、KAWARIから利用できます。SSUはSATORI同梱実装を維持しています。未知のmacOS用標準ABIモジュールとWindows DLLにも読み込み経路はありますが、Windows固有機能への依存まで吸収するものではありません。
